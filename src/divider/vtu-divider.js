import { VtuComponent } from '../assets/package/component';
import {behavior_divider} from '../assets/behaviors/basic';
VtuComponent({
  externalClasses: ['font-class'],
  behavior:  behavior_divider(),
  /**
   * 组件的属性列表
   */
  properties: {
  }
})
